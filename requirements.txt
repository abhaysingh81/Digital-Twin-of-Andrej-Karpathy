# Karpathy Digital Twin — Dependencies
# Install with: pip install -r requirements.txt

# ── Core LLM & Embeddings ─────────────────────────────
google-generativeai>=0.8.0        # Gemini 2.5 Flash + text-embedding-004

# ── Vector Database ───────────────────────────────────
chromadb>=0.5.0                   # Persistent vector store with metadata filtering

# ── Web Interface ─────────────────────────────────────
streamlit>=1.35.0                 # Interactive chat UI

# ── Data & Utilities ──────────────────────────────────
python-dotenv>=1.0.0              # .env API key loading
pathlib2>=2.3.7                   # Path utilities (Python 3.4+ stdlib, but pinned here)
tenacity>=8.2.0                   # Retry logic for API calls

# ── Optional: scraping Karpathy's corpus ─────────────
# requests>=2.31.0
# beautifulsoup4>=4.12.0
# youtube-transcript-api>=0.6.0   # Pull YouTube transcripts
# markdownify>=0.12.0             # HTML → Markdown conversion
